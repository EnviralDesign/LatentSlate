# Rust / Cargo
/target/
**/*.rs.bk

# Environment & Secrets
.env
.env.local
.env.*.local
credentials.json
**/credentials.json
*credentials*.json
*.secret
*.secrets
*.key
*.apikey

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Build artifacts
*.exe
*.dll
*.so
*.dylib
*.pdb

# Logs
*.log
logs/

# User projects (contains media assets, project.json, etc.)
# These are created by the application and should not be committed
/projects/
/test-projects/

# Repo-local LatentSlate runtime state.
# Keep the folder structure visible, but keep user providers, credentials, and
# caches out of git.
/.latentslate/*
!/.latentslate/.gitkeep
!/.latentslate/providers/
/.latentslate/providers/*
!/.latentslate/providers/.gitkeep
!/.latentslate/secrets/
/.latentslate/secrets/*
!/.latentslate/secrets/.gitkeep
!/.latentslate/cache/
/.latentslate/cache/*
!/.latentslate/cache/.gitkeep

# Portable app data root used next to the executable. Ignore it here too in
# case LATENTSLATE_HOME points at the repo during development.
/LatentSlateData/

# Local reference material belongs in the external diagnostics workspace.
/workflows/
/reference/
/evidence/

# OS generated
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

.tmp/*

.venv
generate-git-history-video-here.bat
/.playwright-cli
/.codex-remote-attachments
